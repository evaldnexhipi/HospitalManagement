{% extends 'user/admin/admin_profile.html.twig' %}
{% block trupi %}
    <div class="row">
        <div class="col-lg-6 equel-grid">
            <div class="grid">
                <p class="grid-header">Menaxho Rezervim</p>
                <div class="grid-body">
                    <div class="item-wrapper" id="form2">
                        <div class="form-group row showcase_row_area">
                            <div class="col-md-3 showcase_text_area">
                                <i class="fa fa-phone" aria-hidden="true"></i>
                                <label for="user_form_telepone">Telefoni</label>
                            </div>
                            <div class="col-md-9 showcase_content_area">
                                {{ form_row(userForm.telephone) }}
                            </div>
                        </div>

                        <div class="form-group row showcase_row_area">
                            <div class="col-md-3 showcase_text_area">
                                <i class="fa fa-address-card-o" aria-hidden="true"></i>
                                <label for="user_form_address">Adresa</label>
                            </div>
                            <div class="col-md-9 showcase_content_area">
                                {{ form_row(userForm.address) }}
                            </div>
                        </div>



                        <button type="submit" class="btn btn-sm btn-success">Ndrysho</button>
                        {{ form_end(userForm) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!--HISTORIKU I REZERVIMEVE -->
    <div class="row">

    </div>
{% endblock %}